# Security

## Defaults

- Backend binds to `127.0.0.1` in local commands.
- CORS allows only the configured frontend origin.
- Debug endpoints are available only when `APP_ENV=development`.
- No model files or secrets should be committed.

## Reporting

Open a private security advisory or contact the project owner before public disclosure.
